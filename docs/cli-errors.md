# CLI Errors

`coral-cli` writes a text error envelope to `stderr` for all commands. The envelope is human/LLM-facing and contains only the public message, stable tags, and optional authored remediation. Backend diagnostic context stays out of this default text surface. Parsers should not assume the message/head portion is single-line because Commander-surfaced errors may contain newlines.

`wait` and follow-mode commands keep successful human-readable text on `stdout`. Only failures move to `stderr`.

## Envelope

CLI errors use this shape:

```text
timeoutSeconds: Number must be less than or equal to 1200 [code=invalid_request, http=400]
```

Fields:

| Field         | Type             | Meaning                                                                                                                                                                  |
| ------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `message`     | string           | Human-readable summary. It is not guaranteed to stay on one line                                                                                                         |
| `code`        | string           | Stable programmatic error code, emitted inside the trailing tag block                                                                                                    |
| `http`        | number, optional | HTTP status when the error came from a backend HTTP response                                                                                                             |
| `remediation` | string, optional | Operator-actionable hint included by backend error paths (e.g. `kb_initializing`, `kb_offline`). Surfaced after the message so wrappers can present a concrete next step |

Backend HTTP errors are lifted into the same envelope. Transport-only diagnostic fields are not rendered.

Backend IPC errors are lifted the same way. Mutating and live commands reach the coordinator over IPC, and a JSON-RPC rejection carries its public `{code, message, remediation?}` in the error data. The CLI reads that payload, so an IPC rejection surfaces its own code and exit code rather than a generic internal failure. When an IPC rejection carries no recognized `code`, the CLI falls back to `ipc_rpc_error`.

When a backend HTTP response body is missing, non-JSON, or does not carry a `code`/`message` field, the CLI falls back to:

- `code`: `backend_error`
- `message`: the client-side HTTP status description (e.g. `HTTP 503 Service Unavailable`)

This only affects malformed or truncated backend responses. In normal operation every backend error path emits `{code, message}` and is surfaced unchanged.

## Code Catalog

| Code                                          | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `invalid_usage`                               | CLI-side validation failure before the request is sent. Includes local flag parsing, local integer parsing, and Commander usage errors                                                                                                                                                                                                                                                                                                                         |
| `invalid_request`                             | Backend request/schema validation failure after the request reached the backend                                                                                                                                                                                                                                                                                                                                                                                |
| `invalid_work_directory`                      | A work directory is empty, missing, not a directory, or otherwise cannot be resolved canonically. The message names the supplied path, its resolution base, and the filesystem reason; Coral never falls back to the raw spelling                                                                                                                                                                                                                              |
| `not_found`                                   | Generic backend resource not found                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `session_not_found`                           | Referenced session does not exist                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `audit_requires_ended_session`                | Audit/detail view was requested for a discuss session that has not ended yet                                                                                                                                                                                                                                                                                                                                                                                   |
| `scope_mismatch`                              | The requested resource exists but is outside the caller's scope. Two producers emit it: a resource-binding refusal (`Principal is not bound to the requested resource`), and an explicit job request whose `work_dir` the caller's directory does not contain (`Jobs are outside the caller's work directory scope`). The refused job's own work directory is not disclosed; its remediation points at `coral-cli jobs`, which is unscoped and groups every job under the directory it ran in                                                                                                                                                                                                                                                                                                                                                                                                              |
| `unauthorized`                                | Backend token/auth mismatch                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `missing_capability`                          | The authenticated principal is bound to the resource but lacks the capability the command requires. A nested (child) Coral session gets a message directing the operator to run the command from the top-level session                                                                                                                                                                                                                                         |
| `child_credentials_incomplete`                | A nested Coral command ran with a partial `CORAL_*` child binding, so no request was sent. Raised CLI-side before coordinator discovery, ensure, or dispatch                                                                                                                                                                                                                                                                                                   |
| `ipc_rpc_error`                               | Fallback for a coordinator IPC rejection whose error data did not provide a recognized `code`                                                                                                                                                                                                                                                                                                                                                                  |
| `provider_binding_*`                          | Typed provider authority failure. Current suffixes are `missing_profile`, `profile_unavailable`, `identity_unavailable`, `profile_mismatch`, `subject_mismatch`, `unsupported_selection`, and `invalid_persisted_binding`                                                                                                                                                                                                                                      |
| `provider_scope_missing`                      | The captured caller or durable operation scope does not contain every provider the operation can launch                                                                                                                                                                                                                                                                                                                                                        |
| `system_provider_scope_invalid`               | Daemon startup received a malformed, incomplete, unknown-provider, relative-path, or otherwise non-canonical `CORAL_SYSTEM_PROVIDER_SCOPE`                                                                                                                                                                                                                                                                                                                     |
| `system_provider_scope_unconfigured`          | HTTP or daemon-internal provider execution was requested, but the daemon booted without a named system scope                                                                                                                                                                                                                                                                                                                                                   |
| `transient`                                   | Retry-later failure. This covers HTTP `502`/`503`/`504`, resumable wait interruptions, and a wait event the installed CLI cannot decode after bounded retries. The decode message tells the operator to rerun the command or upgrade the installed Coral plugin; raw schema diagnostics are not emitted. CLI-side: any `TransientHttpError` maps to exit `75` via `instanceof` dispatch; `code === 'transient'` or backend `503` bodies also land on exit `75` |
| `backend_shutting_down`                       | The backend is running but draining and refusing new work                                                                                                                                                                                                                                                                                                                                                                                                      |
| `backend_unreachable`                         | The backend could not be reached at all. Typical causes are not-started daemon, refused connection, or transport-level lookup/reset failures                                                                                                                                                                                                                                                                                                                   |
| `kb_initializing`                             | A KB-touching command was issued while the KB daemon runtime is still starting. Transient — the daemon is healthy, only KB is not ready. Maps to HTTP `503` and exit `75` (retry-later); the response carries a `remediation` hint                                                                                                                                                                                                                             |
| `kb_offline`                                  | The KB daemon runtime is offline or failed. The daemon is otherwise healthy. Maps to HTTP `503` and exit `75`; the `remediation` hint asks the operator to restart the daemon                                                                                                                                                                                                                                                                                  |
| `kb_disabled`                                 | A KB-touching command reached a coordinator whose KB daemon runtime was started with `CORAL_KB_ENABLE=0`. The daemon itself is healthy — only KB is off, and a live coordinator is never evicted by a CLI invocation just to pick up a flipped env var. Maps to HTTP `503` and exit `75`; the `remediation` hint names the operator-side restart, because a nested job cannot drain the coordinator its own parent runs on                                     |
| `backend_error`                               | Fallback for a backend error response that did not provide a recognized `code`                                                                                                                                                                                                                                                                                                                                                                                 |
| `internal` / `internal_error`                 | Unhandled CLI-side failure (`internal`) or backend `500` contract response (`internal_error`)                                                                                                                                                                                                                                                                                                                                                                  |
| `coordinator_socket_in_use`                   | An offline operator command found the coordinator socket owned; shut down the daemon before retrying                                                                                                                                                                                                                                                                                                                                                           |
| `coordinator_socket_bind_failed`              | An offline operator command could not bind the coordinator socket because of a path, permission, or platform failure                                                                                                                                                                                                                                                                                                                                           |
| `coordinator_socket_dir_insecure`             | The coordinator socket uses Coral's installation-keyed fallback directory under a host-shared parent, and Coral observed that the fallback path is not one it can keep private to the calling user: `reason` is `foreign` (the path belongs to another user, so only that owner or an administrator can clear it), `unusable` (the path is not a directory), or `unsecurable` (Coral cannot keep the path private here; the message names which observation reached that, since the operator's next step differs by it). A loose mode on a directory this user owns is tightened rather than refused. Refused rather than bound, because the socket is the singleton coordinator lock. Raised on the coordinator's own startup path and on the offline operator commands that take the same socket as a guard, which pass it through rather than re-wrapping it as `coordinator_socket_bind_failed` |
| `coordinator_socket_dir_unverified`           | The coordinator socket uses the same fallback location, but Coral could not establish private ownership: a required create, observation, or tightening operation could not be attempted or failed, an observation succeeded but returned insufficient ownership metadata, or the required caller uid was unusable. This run therefore reached no ownership verdict — exit 75, on the same no-verdict axis as `coordinator_unreachable` and `coordinator_record_unreadable`, rather than the exit `1` of the ownership verdict above. Its remediation distinguishes an operation failure from a successful observation without an owner and from an unusable required owner |
| `startup_bundle_unresolvable`                 | Backend startup, or the offline `backend store-reset discard --target gen2` operator command, could not resolve the running bundle directory required to publish and follow the active-store selection authority                                                                                                                                                                                                                                               |
| `legacy_foreign_generation`                   | The legacy tree belongs to a different or unreadable generation; `store-reset discard --target legacy` always refuses without touching it                                                                                                                                                                                                                                                                                                                      |
| `legacy_source_not_quiescent`                 | A generation-boundary operation observed a live writer: it could not acquire the writer lock or drain the writer lease                                                                                                                                                                                                                                                                                                                                          |
| `legacy_source_writer_observation_unknown`    | A generation-boundary operation could not determine whether a writer was still live. The source remains untouched; restore process or lease visibility and retry                                                                                                                                                                                                                                                                                                |
| `active_store_coordination_invalid`           | A filesystem trust check rejected the active-store coordination directory or selection record, or found a transition entry that could not be safely superseded                                                                                                                                                                                                                                                                                                 |
| `store_newer_incompatible`                    | The active generated store has a valid product version newer than this build. Ordinary startup and the offline discard command route this classification through the active-store selection protocol, which converts it into a V3 incident instead; the code now reaches the CLI only through the store's standalone classification refusal, exercised in current code only by tests calling it directly, not by any live selection or discard path            |
| `store_older_incompatible`                    | The active generated store has an older product version and a different format fingerprint                                                                                                                                                                                                                                                                                                                                                                     |
| `store_corrupt_or_unsupported`                | The active generated store has missing, malformed, corrupt, or otherwise unsupported format metadata                                                                                                                                                                                                                                                                                                                                                           |
| `store_open_contended`                        | SQLite reported that the active generated store is busy or locked during startup. This is an availability refusal, not a content verdict. It maps to HTTP `503` and exit `75`: the hold ends when the other process or transaction releases the SQLite lock, while a condition that persists after every user has released the store is no longer explained by ordinary contention and must be investigated before the store content is diagnosed                        |
| `store_open_unclassified`                     | This build could not classify the store-open failure. The public message stays generic and the low-level cause remains in structured diagnostic `error.context.cause`; the default remediation deliberately names the canonical store path so the operator knows which store to preserve and inspect. It maps to HTTP `500` and exit `70`, not exit `75`: unlike known contention, this build has no basis to promise that retrying can clear an unrecognized classifier failure, so it is reported as an internal classification failure and the store is preserved for diagnosis |
| `store_not_initialized`                       | A read-only or non-daemon caller found no initialized store; only coordinator startup may create it                                                                                                                                                                                                                                                                                                                                                            |
| `kb_commit_corrupt_or_unsupported`            | A KB commit is corrupt, unsupported, or requires a different Coral version                                                                                                                                                                                                                                                                                                                                                                                     |
| `kb_commit_id_invalid`                        | The requested KB commit ID is not one safe filesystem path segment                                                                                                                                                                                                                                                                                                                                                                                             |
| `kb_commit_not_found`                         | The requested KB commit is absent from the active blocking evidence                                                                                                                                                                                                                                                                                                                                                                                            |
| `kb_commit_already_quarantined`               | Retained quarantine evidence already exists for the requested KB commit                                                                                                                                                                                                                                                                                                                                                                                        |
| `kb_commit_quarantine_failed`                 | Coral could not durably move and publish the requested KB commit quarantine                                                                                                                                                                                                                                                                                                                                                                                    |
| `recovery_quarantine_boundary_not_registered` | The requested recovery boundary is not registered for operator retry                                                                                                                                                                                                                                                                                                                                                                                           |
| `recovery_quarantine_subject_not_found`       | The copied recovery key does not name a retained row; rerun `recovery-quarantine list` and copy one current row's exact coordinate                                                                                                                                                                                                                                                                                                                              |
| `recovery_quarantine_revision_changed`        | The copied recovery coordinate is stale because the retained revision changed                                                                                                                                                                                                                                                                                                                                                                                  |
| `recovery_quarantine_continuation_pending`    | The retained row is a durable continuation and direct clear is unsupported                                                                                                                                                                                                                                                                                                                                                                                     |
| `recovery_quarantine_retry_in_progress`       | The canonical coordinator already owns an in-flight retry for the retained row                                                                                                                                                                                                                                                                                                                                                                                 |
| `provider_host_inventory_unavailable`         | A captured owner failed to return a valid complete inventory, or the selected owner's exact inspect/evict call failed after inventory capture. No partial rows or mutation are returned. Retry the original command; if it persists, run `coral-cli backend shutdown`, then retry the original command to start a fresh coordinator.                                                                                                                                           |
| `provider_host_not_found`                     | No live, retained-blocked, or reclamation-failed provider host matches the exact reference or canonical work-directory selector. Rerun `coral-cli backend provider-host list`, then use a currently listed reference                                                                                                                                                                                                                                                               |
| `provider_host_ambiguous`                     | A canonical work directory matches more than one host. For one listed reference, run `coral-cli backend provider-host inspect <ref>` and verify it, then run `coral-cli backend provider-host evict <ref>`; never choose a match by position                                                                                                                                                                                                                     |
| `provider_host_identity_integrity`            | Coral found duplicate owner IDs before selecting a host, or one exact host reference collided across records/owners. Do **not** evict: preserve the complete error output, then run `coral-cli backend status` to capture coordinator state before escalating the integrity failure                                                                                                                                                                               |
| `provider_host_stale`                         | The selected host changed or disappeared before its owner could revalidate the exact reference; Coral did not retarget the operation. Rerun `coral-cli backend provider-host list` and act only on a currently listed reference                                                                                                                                                                                                                                |
| `provider_host_unserviceable`                 | The initial failing job's `coral-cli wait` output preserves the provider's raw failure cause and names the exact blocked host as a copyable `ph1.…` reference; no second placement attempt is required. Run `coral-cli backend provider-host inspect <ref>`, then run `coral-cli backend provider-host evict <ref>` before retrying fresh placement. Eviction may end work already attached to that host                                                       |
| `invalid_store_reset_incident_id`             | `backend store-reset report` received anything other than a canonical lowercase UUID                                                                                                                                                                                                                                                                                                                                                                           |
| `store_reset_incident_not_found`              | No retained incident exists for the requested canonical UUID                                                                                                                                                                                                                                                                                                                                                                                                   |
| `store_reset_incident_limit_exceeded`         | The retained root exceeded the bounded list-entry limit; direct report by known UUID remains available                                                                                                                                                                                                                                                                                                                                                         |
| `store_reset_build_mismatch`                  | The installed backend, CLI, Claude helper, adjacent manifest, or embedded identity do not form one coherent current build set                                                                                                                                                                                                                                                                                                                                  |
| `store_reset_incident_build_mismatch`         | The retained incident belongs to a different build set and is intentionally unreadable by the current build                                                                                                                                                                                                                                                                                                                                                    |
| `store_reset_reporting_failed`                | A malformed, unsafe, over-limit, I/O, or orchestration condition prevented a safe public report                                                                                                                                                                                                                                                                                                                                                                |
| `coordinator_record_unreadable`               | The coordinator discovery record exists at a named path but could not be read or decoded, or startup decoded a relative shipped socket string whose launcher working directory is unknowable. This does not mean no coordinator is running — retrying re-reads the same unusable record and cannot resolve it. Fix the file's permissions, or delete it if its content is corrupt or stale (a running coordinator never rewrites it), then run any mutating command to relaunch. Raised by coordinator startup before socket authority and by `coral-cli expansion …` (`assertDaemonViewObserved` in `src/cli/expansion/index.ts`), which carries this code as an `InstallError` too                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `coordinator_unreachable`                     | A coordinator discovery record decoded — written only after that coordinator's own IPC listener bound — and its recorded pid was not observed absent, but this command could not reach the coordinator it names. Likely a stale, crashed, or namespace-hidden coordinator, though a live one merely too busy to answer produces the same evidence and cannot be told apart from it. Retry shortly; if it persists, check the recorded pid with `ps` and delete the record file if it is not Coral's coordinator. Raised by the same `coral-cli expansion …` site, and carried the same way                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `handoff_fresh_discovery_unavailable`         | Handoff could not obtain fresh coordinator discovery before or after signaling. Exit `75`; observation `not_observed`; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent because a fresh read can succeed                                                                                                                                                                                                                                                                                                                                                  |
| `handoff_fresh_discovery_changed`             | Fresh discovery named a different incumbent, so handoff must retry against the new owner. Exit `75`; settled observation; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                                     |
| `handoff_signal_capability_unavailable`       | Verified discovery lacked one or more required signal-capability fields. Exit `77`; settled authorization refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                                     |
| `handoff_signal_cooldown_active`              | A previous accepted signal is still inside its measured cooldown, so Coral waits before repeating it. Exit `75`; settled temporary hold; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                        |
| `handoff_legacy_signal_attempt_indeterminate` | A legacy ledger proves that a signal was attempted but not whether the kernel accepted it. Exit `75`; observation `not_observed`; retryable after inspection and cooldown. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                |
| `handoff_shutdown_capability_rejected`        | The incumbent explicitly rejected the shutdown capability. Exit `77`; settled authorization refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                                                    |
| `handoff_shutdown_credential_unavailable`     | Verified discovery had no boot credential with which to request shutdown. Exit `77`; settled authorization refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                                      |
| `handoff_socket_holder_unverified`            | The startup deadline elapsed with the socket still bound and no verified holder pid. Exit `75`; observation `not_observed`; not retryable without operator evidence or recovery. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                         |
| `handoff_manual_policy`                       | `CORAL_HANDOFF_SIGNAL_POLICY=manual` withheld automated signal authority before Coral signaled the incumbent. Exit `77`; settled policy refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                         |
| `handoff_term_only_policy`                    | After accepted SIGTERM and its grace, `CORAL_HANDOFF_SIGNAL_POLICY=term-only` withheld SIGKILL. Exit `77`; settled policy refusal; retryable because the already-signaled target can finish autonomously. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                     |
| `handoff_process_identity_unavailable`        | Coral could not observe the process incarnation and did not establish pid absence. Exit `75`; observation `not_observed`; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                                         |
| `handoff_process_liveness_unknown`            | The target identity matched, but its current liveness could not be observed. Exit `75`; observation `not_observed`; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                                               |
| `handoff_platform_identity_insufficient`      | The platform cannot provide a process identity strong enough to authorize signaling. Exit `77`; settled platform-capability refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                      |
| `handoff_published_incarnation_missing`       | The incumbent published no incarnation, so Coral cannot prove that the pid identifies it. Exit `77`; settled identity-evidence refusal; not retryable. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                   |
| `handoff_published_incarnation_mismatch`      | The observed pid is not the process incarnation published by the incumbent. Exit `75`; settled ownership change; retryable against fresh discovery. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                       |
| `handoff_signal_anchor_missing`               | No authenticated process-identity baseline was observed for the pid. Exit `75`; observation `not_observed`; retryable in a new attempt. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                                                      |
| `handoff_pid_recycled`                        | The pid was recycled after Coral observed the coordinator, so a fresh attempt must identify the current owner. Exit `75`; settled ownership change; retryable. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                                                                              |
| `handoff_signal_rejected_live`                | SIGTERM or SIGKILL was rejected and the verified target remained alive. Exit `77`; settled authorization refusal; not retryable with the same authority. `backend status` prints `Retryable: no`; SessionStart suppresses the notice while the incumbent is alive and emits a code-only notice after the incumbent is absent                                                                                                                                                                                                                                                                                                                                |
| `handoff_accepted_signal_target_alive_after_failure` | Another handoff operation failed after an accepted signal, before Coral observed the target gone. Exit `69`; settled backend-unavailable refusal; retryable because the already-signaled target can finish shutting down. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent. The nested failure remains private diagnostic evidence                                                                                                                                                                                                                                           |
| `handoff_accepted_signal_target_alive_after_bind` | The socket became bindable after an accepted signal, but the verified target remained alive. Exit `69`; settled backend-unavailable refusal; retryable because the already-signaled target can finish shutting down. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                                      |
| `handoff_sigkill_grace_target_gone_socket_still_bound` | The verified target was gone after accepted SIGKILL and its grace, but the socket remained bound. Exit `75`; settled ownership evidence; retryable. Retry the original mutating command so Coral re-observes ownership and removes the socket only if it proves stale. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                        |
| `handoff_sigkill_grace_target_alive`          | The verified target remained alive after accepted SIGKILL and its grace, as can occur during uninterruptible I/O. Exit `69`; settled backend-unavailable refusal; retryable because SIGKILL can take effect once that I/O finishes. `backend status` prints `Retryable: yes`; SessionStart suppresses the notice while the incumbent is alive and after it is absent                                                                                                                                                                                                                                                                                           |

Socket-handoff refusals are documented `CoralSetupError` instances whose code names the observed cause. Catalog-backed
public projections such as `backend status` ignore persisted prose and regenerate the code-indexed, authored message
and remediation. SessionStart cannot import that catalog, so it validates and prints only the bounded error code and
points to `backend status`; arbitrary exception text and persisted diagnostic prose remain private evidence, including
a nested cause attached to a handoff failure.

Provider endpoint construction reports a settled ownership, entry-type, or parent-permission refusal as `proxy_endpoint_insecure`, with `refusal` set to `foreign`, `unusable`, or `unsecurable`. It reports no settled verdict as `proxy_endpoint_unverified`, with `refusal` set to `unverified`: an operation may have failed, a successful observation may have reported no owner, or the fallback address may have named an unusable uid. Consumers must not interpret that code as an insecure-directory verdict.

The coordinator's schema-V1 `startup-diagnostic.json` carries a boolean `retryable` derived from the documented setup-error registry. `store_open_contended` writes `true`; undocumented failures and documented codes without an authored retry policy write `false`. `backend status` accepts both values, preserves the boolean in `recent_failure`, and renders it. This is intentionally a tolerant same-address extension: an older build already rejects `retryable: true` as an inapplicable diagnostic and falls back to its ordinary no-daemon status instead of failing startup. That rollback loses the recent-failure rendering, but the diagnostic file and coordinator log remain available; moving the same payload to another address would make it equally invisible to that shipped reader without improving safety.

Provider-host `list` and `inspect` can report `reclamation-failed`. This status means the coordinator did not complete a provider-host close or recorded-containment reclamation, not that the host was classified broken. Its host metadata always names `reclamationAttempts`, `reclamationFailure`, and `reclamationRetryable`. Before a process handle exists, `pid` and `processGroupId` are absent; after a process is observed but before group authority is recorded, only `pid` is present; recorded containment carries both values. Use `reclamationRetryable`, not field presence, to choose the response. For a retryable reap failure, `provider-host evict <ref>` joins an in-flight attempt or starts another and clears the row only after absence is confirmed. Identity-verification and pre-containment failures are not retryable, and eviction cannot recreate missing process authority; preserve the inspect output and escalate rather than discarding that evidence by restarting.

## Exit Codes

| Exit | When used                                                                                                                                                                                                                                                                                                                                    |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `2`  | `invalid_usage`, `invalid_store_reset_incident_id`                                                                                                                                                                                                                                                                                           |
| `1`  | User-correctable backend/domain errors such as `invalid_request`, `not_found`, `session_not_found`, `audit_requires_ended_session`, `scope_mismatch`, `unauthorized`, `kb_unavailable`, provider-host errors other than `provider_host_inventory_unavailable`, the generation-boundary/operator codes whose rows below say `1`, `store_reset_incident_not_found`, `store_reset_incident_limit_exceeded`, and default `backend_error` cases |
| `75` | Temporary holds and no-verdict refusals: `transient`, `backend_shutting_down`, `kb_disabled`, `kb_initializing`, `kb_offline`, `store_open_contended`, `provider_host_inventory_unavailable` (matched by code name because the IPC path carries no HTTP status), and `coordinator_unreachable`/`coordinator_record_unreadable`/`coordinator_socket_dir_unverified`, plus `legacy_source_writer_observation_unknown` when a generation's writer lease could not be observed at all, the handoff codes `handoff_fresh_discovery_unavailable`, `handoff_fresh_discovery_changed`, `handoff_signal_cooldown_active`, `handoff_legacy_signal_attempt_indeterminate`, `handoff_socket_holder_unverified`, `handoff_process_identity_unavailable`, `handoff_process_liveness_unknown`, `handoff_published_incarnation_mismatch`, `handoff_signal_anchor_missing`, `handoff_pid_recycled`, and `handoff_sigkill_grace_target_gone_socket_still_bound`, plus generic HTTP `503` fallback. Documented-code exits come from `DOCUMENTED_CORAL_SETUP_ERRORS` through `errorCodeToExit`; `coral-cli expansion` delegates error results to that same mapper. `store_open_contended` is positive evidence of contention and is deliberately not a member of `NOT_OBSERVED_CORAL_SETUP_ERROR_CODES`. Among the handoff codes, only `handoff_fresh_discovery_unavailable`, `handoff_legacy_signal_attempt_indeterminate`, `handoff_socket_holder_unverified`, `handoff_process_identity_unavailable`, `handoff_process_liveness_unknown`, and `handoff_signal_anchor_missing` carry observation `not_observed`. Retry only when that code's remediation says the condition can clear without intervention |
| `69` | `backend_unreachable`, `handoff_accepted_signal_target_alive_after_failure`, `handoff_accepted_signal_target_alive_after_bind`, and `handoff_sigkill_grace_target_alive`                                                                                                                                                                                                                            |
| `77` | Authorization failures that no retry fixes: `missing_capability`, `child_credentials_incomplete`, `handoff_signal_capability_unavailable`, `handoff_shutdown_capability_rejected`, `handoff_shutdown_credential_unavailable`, `handoff_manual_policy`, `handoff_platform_identity_insufficient`, `handoff_published_incarnation_missing`, and `handoff_signal_rejected_live`. `handoff_term_only_policy` also exits `77`, but is retryable because Coral already delivered SIGTERM and the target can finish autonomously. The same code is used by `coral-cli expansion …`, whose single-JSON-line output carries `missing_capability` and `child_credentials_incomplete` as an `InstallError` |
| `70` | `internal`, `internal_error`, `store_open_unclassified`, `store_reset_build_mismatch`, `store_reset_incident_build_mismatch`, `store_reset_reporting_failed`, and generic HTTP `500` fallback                                                                                                                                                |

`kb_unavailable` deliberately remains exit `1` when mapped by code alone. Its remediation requires equipping the
missing engine rather than waiting for an observed temporary hold, and this preserves the code-only mapping from
before documented exits moved into the registry. A transported HTTP `503` still contributes the generic exit
`75`, as it did before centralization; documented codes with an explicit non-default exit keep that authored
exit. The registry sweep found no other existing code whose code-only exit changed: `startup_not_ready`, `kb_initializing`, `kb_offline`,
`coordinator_unreachable`, `coordinator_record_unreadable`, and `coordinator_socket_dir_unverified` remain `75`;
all other pre-existing documented codes remain `1`. The new `store_open_contended` and
`store_open_unclassified` codes deliberately add exits `75` and `70` respectively.

`coral-cli backend shutdown` also exits `75` when the command has no verdict about whether a coordinator stopped. That group includes an unreadable discovery record (`unreadable_record`); a missing record while the current socket is present (`no_record_socket_present`); a refused connection (`socket_refused`); a response that did not resolve the shutdown request (`refused_by_response`); a request that never completed (`no_response`); and both absence-evidence outcomes. `no_record` found neither a discovery record nor this build's current socket, but that file evidence can miss a live v0.10.9 coordinator at an unenumerated custom fallback before it publishes. `recorded_process_absent` directly confirms that the process named by the record is gone, but the record may be stale while a different coordinator has bound its socket and not yet published its own record; the v0.10.9 custom-fallback window is one way this happens, not the only one. The formatter therefore states the evidence for each outcome and explicitly declines to report that the backend stopped. `socket_refused` belongs to the same group because an absent recorded pid short-circuits before any request is sent, so the refusal proves only that nothing answered at that exact socket at that moment; a coordinator's HTTP listener can also close at the top of its drain while its confirmed-live process continues through IPC close and store finalizers. The remaining refusals exit `1`: `capability_rejected` is a coordinator that answered and declined, while `nested_child` is a stable local prohibition that refuses before observing the coordinator because a nested Coral child must never drain the coordinator its parent job runs on (see [Nested Coral Commands](#nested-coral-commands)). The distinction is load-bearing because [Configuration](./configuration.md) requires a successful shutdown before `store-reset discard` or `kb-commit quarantine`: only exit `0` authorizes the destructive follow-up. Exit `75` means "do not proceed without a verdict," not necessarily "retry until success." For example, `unreadable_record` remains `75` until a person repairs or removes the discovery record. Retry only where the specific remediation says the evidence can change without intervention. A caller running under `set -e` stops on every refusal, as it should; it must not reinterpret either absence-evidence row as permission to continue. The full mapping is `SHUTDOWN_REFUSAL_EXIT_CODES` (`src/cli/commands/backend.ts`), which is exhaustive over `ShutdownReason` so a new refusal cannot inherit either answer by default.

`coral-cli backend provider-proxy-set contain <pps1-token>` is an exact-set operation and does not change the
shutdown reason, mode, or exit code. `backend status` supplies the token and live-claim count. Its result and
claim-discharge result are separate discriminators, with this exhaustive treatment:

| Result | Exit | Destructive effect and required next step |
| ------ | ---- | ----------------------------------------- |
| `contained` + `completed` | `0` | Recorded-set reaping may have signalled the recorded proxy process group and recorded provider roots; claim delivery and capsule retirement completed. Run `coral-cli backend status` to read the resulting state. |
| `contained` + `initial-disposition-retry-owned` | `75` | Recorded-set reaping already completed, but Coral still represents the set while the first successor disposition is pending. Keep the coordinator running and rerun `coral-cli backend status`; do not repeat containment against a release-delivery hold. |
| `contained` + `operational-retry-owned` | `75` | Recorded-set reaping already completed, but one or more reported delivery/retirement incidents remain retry-owned. Keep the coordinator running, inspect the reported incident and coordinator log, repair that consumer or retirement dependency, then rerun `coral-cli backend status`. |
| `abandoned` + `completed` | `0` | No process was signalled; Coral released its representation and completed claim delivery and capsule retirement without asserting absence. Verify the proxy, guardian, reaper, and provider processes through external process controls. |
| `abandoned` + `initial-disposition-retry-owned` | `75` | No process was signalled; abandonment began, but Coral still represents the set while the first successor disposition is pending. Keep the coordinator running and rerun `coral-cli backend status`. |
| `abandoned` + `operational-retry-owned` | `75` | No process was signalled; abandonment began, but a reported delivery/retirement incident remains retry-owned. Inspect the incident and coordinator log, repair that dependency, then rerun `coral-cli backend status`. |
| `unattributable-group-abandoned` + `completed` | `0` | The recorded leader identity was gone and the surviving or unobservable numeric group could not be attributed. The preceding reap attempt may already have signalled verified targets; abandonment sent no additional signal and released representation without asserting absence. Claim delivery plus capsule retirement completed. Verify the proxy and provider processes externally, then run `coral-cli backend status`. |
| `unattributable-group-abandoned` + `initial-disposition-retry-owned` | `75` | The preceding reap attempt may already have signalled verified targets; abandonment sent no additional signal and asserted no absence. Coral still represents the set while the first successor disposition is pending. Keep the coordinator running and rerun `coral-cli backend status`. |
| `unattributable-group-abandoned` + `operational-retry-owned` | `75` | The preceding reap attempt may already have signalled verified targets; abandonment sent no additional signal and asserted no absence. A reported delivery/retirement incident remains retry-owned. Repair that dependency and rerun `coral-cli backend status`. |
| `set-not-found` | `1` | Nothing was signalled or released. Run `coral-cli backend status` and copy the current exact token. |
| `not-held` | `1` | Nothing was signalled or released. If the reported state is `available` or `draining`, use ordinary drain; for any acquisition, recovery, or release-delivery state, do not repeat containment. Rerun `coral-cli backend status` and let that named owner finish. |
| `deadline-pending` | `75` | Nothing was signalled or released. For `reattaching`, the remaining time is the hold's recorded adoption-window deadline; for fault-driven `containing`/`containment-wait`, it is the fixed 30-second containment-attempt observation gate. Wait the printed duration, then rerun the exact contain command. |
| `authorization-stale` | `75` | The result's effect fields say whether SIGTERM/SIGKILL was sent and whether recorded-containment absence was confirmed before the held attempt changed. The stale capability never releases representation. Run `coral-cli backend status` before retrying the current hold. |
| `enforcer-alive` | `75` | Nothing was signalled or released. After external verification, rerun the exact command with `--abandon-without-absence` to release representation without asserting absence. |
| `enforcer-unobservable` | `75` | Nothing was signalled or released. Restore process observation and rerun the exact command, or externally verify the process set and rerun with `--abandon-without-absence`. |
| `recorded-group-unattributable` | `75` | The recorded leader identity is gone, but the numeric group remains alive or unobservable and cannot be attributed. The reap attempt may already have signalled verified targets; representation was not released. After external verification, rerun the exact command with `--abandon-without-absence`; that sends no additional signal and releases representation without asserting absence. |
| `store-unreadable` | `75` | Nothing was signalled or released, and `--abandon-without-absence` cannot override this fence. Run `coral-cli backend recovery-quarantine list`. Repair the exact raw row externally, or—only when the list prints one—run its exact `discard-provider-operation` command. A successful discard atomically removes the raw row, due pointers, and quarantine evidence; rerun the list and then status. |
| unsupported coordinator (`-32601`) | `75` | The coordinator rejected the method before accepting the operation, so nothing was signalled or released. Upgrade or restart into this build, then rerun `coral-cli backend status` before retrying. |
| coordinator draining | `75` | The coordinator refused the request before dispatch, so nothing was signalled or released. Wait for the successor, then rerun `coral-cli backend status` before retrying. |
| unsupported coordinator result | `75` | The coordinator replied, but this CLI could not decode the result. Whether a signal or representation release occurred is unknown. Upgrade or converge on one build, then rerun `coral-cli backend status` before any retry. |
| timeout | `75` | The bounded request deadline expired without a reply. Whether a signal or representation release occurred is unknown. Rerun `coral-cli backend status` before deciding whether to retry. |

An older coordinator's JSON-RPC method-not-found, a structurally identified newer result, a pre-dispatch drain,
and a bounded request timeout all become named `75` no-verdicts. An unfamiliar result or timeout may arrive
after a signal or representation release, so both require `coral-cli backend status` before any retry. None is rendered as
an internal parse error. Forced containment may signal the recorded proxy process group plus every recorded provider root for the set that is
observed present. Guardian and reaper are never signalled; the contained outcome was authorized only after both
were observed absent. An `--abandon-without-absence` outcome may leave either enforcer live because that mode
releases representation without signalling or asserting process absence.

`coral-cli backend recovery-quarantine list` prints `discard-provider-operation` only for the exact key and
fingerprint of a persisted, active unreadable-provider-operation quarantine. The coordinator first claims that
subject as the operator-discard owner, then removes the raw row, every known-generation due pointer, and the
quarantine evidence in one transaction. Its complete result and no-verdict treatment is:

| Result | Exit | Destructive effect and required next step |
| ------ | ---- | ----------------------------------------- |
| `discarded` | `0` | The exact raw row, its due pointers, and quarantine evidence were permanently removed together. No process was signalled and no operation was settled. Rerun `coral-cli backend recovery-quarantine list`, then `coral-cli backend status`. |
| `absent` | `1` | The claimed raw row was already absent; the temporary discard claim was released and nothing was removed. Rerun `coral-cli backend recovery-quarantine list`. |
| `readable` | `1` | This build can now decode the raw row; the temporary discard claim was released and nothing was removed. Use the exact `clear` command rendered by the refusal so ordinary recovery owns the retry. |
| `revision-mismatch` | `75` | Either the persisted quarantine subject or the raw row no longer matches the requested fingerprint. Nothing was removed and any temporary discard claim was released. Rerun `coral-cli backend recovery-quarantine list` and inspect the new exact coordinate. |
| `quarantine-not-found` | `75` | No exact persisted quarantine subject authorized the operation. The raw row and due pointers were untouched. Start or repair the canonical coordinator, rerun `coral-cli backend recovery-quarantine list`, and use only a command it currently prints. |
| `owned` (`retrying` or `continuation`) | `75` | Recovery already owns the subject. The raw row, due pointers, and quarantine evidence were untouched. Let that owner finish, then rerun `coral-cli backend recovery-quarantine list`. |
| unsupported coordinator (`-32601`) | `75` | The coordinator rejected the method before accepting the discard operation, so nothing was deleted. Upgrade or restart into this build, rerun `coral-cli backend recovery-quarantine list`, and retry only a currently printed command. |
| coordinator draining | `75` | The coordinator refused the request before dispatch, so nothing was deleted. Wait for the successor, then rerun `coral-cli backend recovery-quarantine list` before deciding whether to retry. |
| unsupported coordinator result | `75` | No discard verdict: the coordinator replied, but the response could not be decoded, whether its coordinates are missing, malformed, mismatched, or exact. The requested coordinate is retained; deletion may already have completed. Upgrade or converge on one build, then rerun `coral-cli backend recovery-quarantine list` and `coral-cli backend status` before any retry. |
| timeout | `75` | No discard verdict: the bounded request deadline expired without a reply, and deletion may already have completed. Rerun `coral-cli backend recovery-quarantine list` and `coral-cli backend status` before deciding whether to retry. |

Synthetic entries derived only from an unreadable raw row, and persisted entries in `retrying` or
`continuation`, never print a discard command. Every received but undecodable discard response retains the
requested coordinate and becomes the same named no-verdict because the destructive request may have completed.

`coral-cli backend status` sets an exit code too, for the same reason `backend shutdown` does: `backend status && <destructive op>` used to read every outcome, including "state is unknown," as permission to proceed. Its daemon-status contribution is `0` for `ok`, `shutting_down`, `unauthorized`, `recent_failure`, `no_record_no_socket`, and `recorded_process_absent`, and `75` for `foreign_coordinator`, `undecodable_record`, `unreachable`, and `no_record_socket_present`. `no_record_no_socket` reports that neither a discovery record nor a socket at the current expected address was found. `recorded_process_absent` reports that the process named by the record was observed absent, while allowing that the record may be stale and another coordinator may hold the socket before publishing its own record. Their `0` reports a completed read and does not authorize a destructive follow-up. `foreign_coordinator` instead identifies a decoded coordinator for another namespace or flavor at the recorded address; startup remains held until that ownership conflict is resolved, so it contributes `75`. `no_record_socket_present` remains the unresolved boot-race/stale-socket evidence that the coordinator's own IPC socket exists with no record written yet. The command also composes its process-local handoff preflight: of the eight production-reachable routing bases, `same-build-set` and `incumbent-absent` contribute `0`, while the other six contribute `75`; an abandoned handoff whose stdout did not drain also contributes `75`. The composition helper additionally maps no live result and a display-only continuation to `0`, but those are isolated/default test-seam states, not states that reach the production `backend status` action. Only `incumbent-unresolved` is not-observed evidence; the other five nonzero routing-basis contributions mark decisive conditions requiring operator attention, including the ordinary same-version/different-build-set case. For `incumbent-unresolved{cause:'health-shape-rejected'}`, daemon status may still render the peer as healthy, so the successor does not delegate to daemon remediation: run `coral-cli backend shutdown`, then run any `coral-cli` mutating command (or start a Claude Code session) to attempt startup or handoff from the current installation. Any provider-proxy-set row this build cannot decode—including an unfamiliar nested enforcer observation or malformed row-local field—is skipped, contributes `75`, and retains the exact containment command when its token and identity remain valid. A non-array `providerProxySets` collection still rejects the health shape because that failure is not row-local. Fully understood unresolved provider-proxy-set rows also contribute `75`, so a known held set cannot let `backend status && <destructive op>` continue. Known rows render the guardian and reaper observations separately whenever a status disposition carries them. The other two unresolved causes continue to use the daemon-status remediation.

The same local composition includes the routing journal and this process's publication incidents. The journal classifier has eleven exact arms: `absent`, `vacant`, `uninitialized`, `detached-wal`, `generation-missing`, `foreign-generation`, `format-mismatch`, `schema-divergent`, `current`, `unreadable`, and `undeterminable`. `absent`, `vacant`, and `uninitialized` contribute `0`; the seven artifact-refusal arms contribute `75`; `current` remains content-dependent. A zero-byte `vacant` journal names both interrupted creation and truncation as consistent causes, claims neither, and has no successor. `undeterminable` names retry without discard because ambiguous observation authorizes neither publication nor quarantine. A current journal contributes `75` for an absent unresolved owner, an unobservable unresolved owner except `probe-not-available`, or an exact `selection-evicted-at-capacity` retirement; otherwise it contributes `0`. Every committed terminal remains history with exit contribution `0`.

Publication keeps a separate outcome axis. Each classifier refusal is `artifact-refused{classification}` and retains the complete classification object; its successor comes from that classification's policy row. `not-published{invalid-record}` contributes `70`; `contended`, `generation-maintenance`, `capacity-exhausted`, `io-failed`, `storage-corrupt`, `rejected-transition`, and `coordination-unavailable` contribute `75`. `commit-outcome-unknown` is used only after commit starts, contributes `75`, and explicitly warns that the commit may have landed. Local contributions use precedence `70` over `75` over `0`. Every publication incident retains its invocation ID and terminal incidents retain the terminal disposition being recorded.

`detached-wal`, `generation-missing`, `foreign-generation`, `format-mismatch`, `schema-divergent`, and `unreadable` are decisive `75` classifications whose successor is `coral-cli backend routing-status discard`. Discardability is derived from the classifier policy table. `absent`, `vacant`, `uninitialized`, `current`, and `undeterminable` refuse discard; the first three exit `0` because no retained application journal needs removal, while `current` and `undeterminable` exit `75`. When both guarded observations find the main absent beside a non-empty WAL, discard succeeds with a wal-only incomplete quarantine; a later discard remains blocked until that exact entry is cleared. A completed discard exits `0`.

Every expected routing-status discard refusal is typed and operator-visible. Socket and maintenance refusals exit `75` and print the specific shutdown, storage repair, later retry, process-observation repair, or generation-root repair successor. An unobservable quarantine listing or target coordinate exits `75`; its successor is to repair the reported storage condition and rerun `coral-cli backend routing-status discard`. An incomplete retained set exits `75` and names `coral-cli backend routing-status quarantine clear --id <id>` before another discard. Capacity exhaustion exits `75` and names `coral-cli backend routing-status quarantine list`, exact clears, and a discard retry. `quarantine-coordinate-occupied` exits `75`, preserves the existing coordinate, and says to rerun discard; a repeating coordinate requires repair of the quarantine ID source.

Discard storage outcomes distinguish retention certainty and effect durability. `quarantine-storage-failed` exits `75` with a known retained set; its moved and removed arrays contain only directory-barriered effects, while separate observed arrays carry successful namespace changes whose barriers failed. A known-empty result with no observed effect says that no artifact is retained and names storage repair followed by `coral-cli backend routing-status discard`. Any observed effect is rendered as uncertain and names storage repair followed by `coral-cli backend routing-status quarantine list`; if that list contains the printed ID, the message names `coral-cli backend routing-status quarantine clear --id <id>` before the discard retry. A known non-empty result without an observed effect names quarantine list, storage repair, clear, and then discard. `quarantine-retention-undeterminable` exits `75` and says that whether evidence was retained could not be determined; its required successor is storage repair followed by quarantine list and the same conditional clear before discard. The typed storage causes include `root-create-failed`, `artifact-move-failed`, `artifact-observation-failed`, `directory-sync-failed`, and `ownership-lost`; the observation failure also prints its errcode. These outcomes cover both refusals before an artifact effect is established and partial or uncertain outcomes after an attempted effect; neither category throws merely because no artifact has yet been moved or removed.

Routing-status quarantine retains at most 16 complete or incomplete artifact sets and never silently prunes them. `coral-cli backend routing-status quarantine list` shows each visible exact ID, path, artifact set, and completeness. An undeterminable list exits `75` and names storage repair followed by another `coral-cli backend routing-status quarantine list`; a bounded-list overflow exits `75` and names exact clears followed by the same list command. At the ceiling, discard refuses until the operator uses `coral-cli backend routing-status quarantine clear --id <id>` for evidence that is no longer needed. Clear permanently removes one exact set under the same socket and generation-maintenance guards. Every clear guard refusal exits `75`: a live coordinator names `coral-cli backend shutdown` followed by the exact clear retry, socket observability or security names repair followed by that retry, and maintenance contention, unknown writer identity, or lost ownership names its corresponding wait or repair followed by that retry. `quarantine-clear-undeterminable` is a pre-removal refusal: it exits `75`, identifies the artifact that could not be observed, and names storage repair followed by the same exact clear command. `quarantine-clear-storage-failed` covers a failed removal or directory sync whether or not an earlier removal completed; it exits `75`, reports directory-barriered removals separately from observed removals whose barrier failed, reports the synced set, and names quarantine list, storage repair, and the same exact clear command. A missing exact ID is idempotent success with exit `0`, and a complete clear exits `0`. An exact retained `selection-evicted-at-capacity` tombstone contributes `75` because its invocation identity remains actionable through `backend routing-status resolve --invocation <id>`; its rendered line also says whether a terminal was recorded before acknowledgement. A successful acknowledgement folds the exact tombstone into aggregate history and exits `0`. Exact `operator-resolved` tombstones render `owner-absent` for a decisive absence or `operator-abandoned-unobservable` for a forced abandonment, so the latter's weaker owner evidence remains visible even though both are terminal information with exit contribution `0`. Once exact identity expires by acknowledgement or retention pressure, the `retirement-history-truncated` aggregate remains rendered but contributes `0`: the aggregate deliberately retains no identity an operator could act on.

`coral-cli backend routing-status resolve --invocation <id>` preserves publication truth at the outer boundary with distinct `artifact-refused`, `not-published`, and `commit-outcome-unknown` arms. The failure arms are `Readonly<{invocationId: string}> & Exclude<PublicationOutcome, {kind: 'committed'}>`, so the outer discriminator cannot disagree with a nested outcome. `resolved`, `acknowledged-capacity-eviction`, and `already-terminal` exit `0`; `stale` and `live-owner` exit `1`; `unauthorized-unobservable`, `status-unavailable`, `artifact-refused`, and `commit-outcome-unknown` exit `75`. `not-published{invalid-record}` exits `70`, and every other `not-published` cause exits `75`. A failed result retains the requested invocation ID, so its successor prints a reusable command. A decisively absent owner may be resolved; a live owner must finish first. An unobservable owner requires `--force-unobservable` only after external verification, and `deadline-expired` cannot be overridden.

`coral-cli wait jobs` reuses exit `75` for a second, unrelated meaning that is not one of the failures above: `src/cli/follow.ts` calls `errorCodeToExit('transient')` when the bounded wait's timeout elapses, or a `waiting` event names still-running jobs, while jobs remain outstanding. That is an expected, benign pause — the jobs are still running, not failing — and the fix is to resume, not retry: rerun with the printed `--cursor` value. See [Skills](./skills.md) and the provider skill files (`clients/skills/*/SKILL.md`), which document this same code as part of the `wait jobs` monitoring contract.

An interrupted live wait also exits `75` with code `transient` when its delegated child ends from a signal, its subscription exhausts transient retries, or its initial subscription attempts exhaust while the backend is recovering or draining. The error keeps the signal or subscription diagnostic as its message and adds an exact remediation command: `coral-cli wait jobs <remaining-job-ids>` before any subscription opens, or `coral-cli wait jobs <remaining-job-ids> --cursor <current-cursor>` after the CLI has observed a wait position. Exhausted `backend_unreachable` attempts remain code `backend_unreachable` with exit `69`; the backend must be made reachable before the wait can resume. A second locally received Ctrl+C remains different: it aborts the jobs and exits `1`; when the first local Ctrl+C is reflected back as delegated `SIGINT`, the CLI reconnects instead of emitting an error.

### Provider-proxy role process exit codes

These are process exit codes for the backend artifact (`coral-backend.cjs`) itself when dispatched into a
guardian, reaper, or proxy role via `--provider-guardian` / `--provider-reaper` / `--provider-proxy
<capsule-path>` (`src/coordinator/bootstrap.ts`) — not `coral-cli` exit codes, and not part of the stderr
error envelope above. They let an operator reading a role process's own exit code tell which role failed to
start without correlating it against a log line, and are distinct per role and from `0` (success), `1` (a
coordinator's own generic startup failure), and `70` (`--print-store-reset-build-identity`'s own strict
identity failure):

| Exit | Role       |
| ---- | ---------- |
| `71` | `guardian` |
| `72` | `reaper`   |
| `73` | `proxy`    |

These three codes cover only a failure inside `runProviderRoleMain` after role dispatch succeeded. Parsing
the role invocation itself (`parseProviderRoleArgv`) happens earlier and is not wrapped in that same
try/catch: a malformed invocation — an unparseable or non-canonical capsule path, two role flags, or two
occurrences of the same flag — throws synchronously out of `main()` and is caught only by the top-level
`.catch()`, which exits `1`. A malformed provider-role argv therefore exits `1`, the same generic code as an
ordinary coordinator startup failure, not a distinct code.

Generation-boundary and offline-operator failures that remain `CoralSetupError` values keep the same CLI exit whether they arrive directly, over IPC, or through the HTTP gateway. Routing-status discard converts its expected socket and maintenance conditions into the command-specific result contract above, so those conditions do not reach this table on that command:

| Code                                | Direct / IPC CLI exit | HTTP status | CLI exit after HTTP lift |
| ----------------------------------- | --------------------- | ----------- | ------------------------ |
| `coordinator_socket_in_use`         | `1`                   | `409`       | `1`                      |
| `coordinator_socket_bind_failed`    | `1`                   | `409`       | `1`                      |
| `coordinator_socket_dir_insecure`   | `1`                   | `409`       | `1`                      |
| `coordinator_socket_dir_unverified` | `75`                  | `409`       | `75`                     |
| `startup_bundle_unresolvable`       | `1`                   | `409`       | `1`                      |
| `legacy_foreign_generation`         | `1`                   | `409`       | `1`                      |
| `legacy_source_not_quiescent`       | `1`                   | `409`       | `1`                      |
| `legacy_source_writer_observation_unknown` | `75`                  | `409`       | `75`                     |
| `active_store_coordination_invalid` | `1`                   | `409`       | `1`                      |
| `store_newer_incompatible`          | `1`                   | `409`       | `1`                      |
| `store_older_incompatible`          | `1`                   | `409`       | `1`                      |
| `store_corrupt_or_unsupported`      | `1`                   | `409`       | `1`                      |
| `store_open_contended`              | `75`                  | `503`       | `75`                     |
| `store_open_unclassified`           | `70`                  | `500`       | `70`                     |
| `store_not_initialized`             | `1`                   | `409`       | `1`                      |
| `kb_commit_corrupt_or_unsupported`  | `1`                   | `409`       | `1`                      |
| `kb_commit_id_invalid`              | `1`                   | `400`       | `1`                      |
| `kb_commit_not_found`               | `1`                   | `409`       | `1`                      |
| `kb_commit_already_quarantined`     | `1`                   | `409`       | `1`                      |
| `kb_commit_quarantine_failed`       | `1`                   | `409`       | `1`                      |

This table states the exit contract for each code if it fires; it does not claim every code is still reachable through ordinary use. `store_newer_incompatible` is the one exception — see the Code Catalog entry above for what actually produces it today.

Store-reset errors never carry `http` or diagnostic context; they carry only a fixed public-safe `remediation` selected by code. They never interpolate the supplied incident argument or a low-level filesystem/SQLite/child-process message. The successful report is written only to `stdout`; Coral creates no report file and performs no upload. If reporting itself fails, paste the complete fixed error output into the Store-reset incident issue form instead of attaching evidence or raw logs.

Provider-routing errors retain one stable code per failed authority check:

| Code                                         | Typical cause                                                               | Operator action                                                                                                                  |
| -------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `provider_binding_missing_profile`           | The required provider is absent from the captured scope                     | Select that provider profile for the caller, or configure it in the named system scope                                           |
| `provider_binding_profile_unavailable`       | The selected credential directory is missing or unreadable                  | Restore it and retry with the same absolute `CODEX_HOME` or `CLAUDE_CONFIG_DIR`                                                  |
| `provider_binding_identity_unavailable`      | The selected Codex profile exposes no consistent ChatGPT workspace identity | Run `CODEX_HOME=/abs/path codex login`, then retry. Codex API-key login is unsupported                                           |
| `provider_binding_profile_mismatch`          | Resume/recovery selected a different canonical profile                      | Retry with the original selector, or start a new session for the other profile                                                   |
| `provider_binding_subject_mismatch`          | A Codex account binding now resolves to another workspace subject           | Reauthenticate the intended Codex profile or start a new session under the new account                                           |
| `provider_binding_unsupported_selection`     | The provider or selector route is not registered                            | Use a registered provider and its documented profile selector                                                                    |
| `provider_binding_invalid_persisted_binding` | Durable state is not valid under the strict current codec                   | Stop using the affected state and start a new session/operation; do not edit or translate the record                             |
| `provider_scope_missing`                     | A multi-provider operation lacks at least one profile it may launch         | Relaunch it from a caller with every required profile selected                                                                   |
| `system_provider_scope_invalid`              | Strict named system-scope validation failed at daemon startup               | Rebuild the JSON with canonical `realpath` results, export it, and restart the daemon                                            |
| `system_provider_scope_unconfigured`         | HTTP/internal execution has no caller scope and no configured system scope  | Configure it, run `coral-cli backend shutdown`, restart through a normal mutating command, and verify `coral-cli backend status` |

The complete setup procedure and profile/account distinction are documented in [Multi-Account Provider Routing](./configuration.md#multi-account-provider-routing).

## Validation Timing Matters

Two failures can both mean "bad input" and still produce different exit codes because they were caught at different stages.

Client-caught example: local CLI validation (e.g. `parseIntegerFlag`) fails before any backend request, producing `invalid_usage` and exit code `2`.

```text
--<flag> must be an integer [code=invalid_usage]
```

Server-caught example: the CLI accepts the flag shape, but backend Zod schema validation rejects the value. The call reaches the backend, which returns an authored `invalid_request` message and exit code `1`; the raw issue list is not printed by the default CLI formatter.

```text
<field>: <constraint> [code=invalid_request, http=400]
```

The input is wrong in both cases. The exit code differs because `invalid_usage` is rejected locally, while `invalid_request` is rejected by backend schema validation.

## `503` vs Unreachable

Two "service unavailable" cases also differ on purpose:

- `backend_shutting_down` means the backend answered and said it is draining. Its exit code is `75`, and this specific condition can clear when the drain finishes.
- `backend_unreachable` means the CLI could not talk to the backend at all. Exit code is `69`, which usually means the daemon needs to be started or restarted first.
- `kb_initializing` and `kb_offline` mean the daemon answered, but the KB daemon runtime specifically is not ready. The daemon itself is healthy — only KB-touching commands surface these. Both map to exit `75`.

Drain-in-progress example:

```text
Backend shutting down [code=backend_shutting_down, http=503]
```

Backend-not-running example:

```text
fetch failed [code=backend_unreachable]
```

## Nested Coral Commands

A `coral-cli` invoked from inside a provider job (a Coral child) reconnects to its parent coordinator and never starts or replaces one. Three failures are specific to that boundary, and all three tell the operator to go back to the top-level Coral session:

- `child_credentials_incomplete` (exit `77`) — the child binding was partial, so the command was rejected locally and no request was sent.
- `missing_capability` (exit `77`) — the child principal is valid but its capability set does not cover the command. The denial is authoritative; the parent's capability set is not negotiable from the child.
- `backend_unreachable` (exit `69`) — the parent coordinator was unreachable, draining, replaced, or too slow. The message states that no coordinator was started or replaced and points at `coral-cli backend status` from the top-level session.

`coral-cli backend shutdown` from a child is refused for the same reason and exits `1`, because a child must not drain the coordinator its parent job is running on.

## Consumer Examples

Retry only on retry-later failures:

```bash
#!/usr/bin/env bash
set -o pipefail

stderr_file="$(mktemp)"
trap 'rm -f "$stderr_file"' EXIT

while true; do
  if coral-cli wait jobs "$JOB_ID" 2>"$stderr_file"; then
    exit 0
  else
    wait_status=$?
  fi

  stderr_text="$(cat "$stderr_file")"
  case "$stderr_text" in
    *"[code=transient"*|*"[code=backend_shutting_down"*)
      sleep 2
      ;;
    *"[code=backend_unreachable"*)
      echo "Coral backend is not reachable; restart it first." >&2
      exit "$wait_status"
      ;;
    *)
      printf '%s\n' "$stderr_text" >&2
      exit "$wait_status"
      ;;
  esac
done
```

Branch in an LLM/tool wrapper by the trailing `code` tag:

```ts
type CliError = { message: string; code: string };

function parseCliError(stderr: string): CliError {
  const match = stderr.match(/\[code=([^,\]]+)/);
  return {
    message: stderr,
    code: match?.[1] ?? 'unknown',
  };
}

function classify(error: CliError): 'fix-input' | 'retry' | 'restart-backend' | 'run-at-top-level' | 'escalate' {
  switch (error.code) {
    case 'invalid_usage':
    case 'invalid_request':
    case 'not_found':
    case 'session_not_found':
    case 'scope_mismatch':
      return 'fix-input';
    case 'transient':
    case 'backend_shutting_down':
      return 'retry';
    case 'backend_unreachable':
      return 'restart-backend';
    case 'missing_capability':
    case 'child_credentials_incomplete':
      return 'run-at-top-level';
    default:
      return 'escalate';
  }
}
```
