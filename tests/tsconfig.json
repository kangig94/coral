{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".."
  },
  "include": ["**/*.ts", "../src/types/**/*.d.ts"],
  "exclude": ["../node_modules", "../dist", "../bridge", "../build"]
}
