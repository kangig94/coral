import { defineRecoverySource, type RecoverySource } from '../../../recovery/containment.js';

type RawJob = { readonly id: string };

function scanCoordinatorJobRecoveryEnvelopes(): readonly RawJob[] {
  return [];
}

export function coordinatorJobRecoverySource(): RecoverySource<RawJob> {
  return defineRecoverySource({ boundary: 'coordinator-job', scan: () => scanCoordinatorJobRecoveryEnvelopes() });
}
