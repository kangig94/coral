# TODO

## Simplification Candidates Requiring Logic Decisions

- `src/coordinator/services/recovery/service.ts:177` and `src/coordinator/services/recovery/service.ts:204` use `Boolean(preservedConversationRef ?? continuity)` while already inside a truthy `continuity` branch. Simplifying this needs a decision on whether an empty `preservedConversationRef` should override otherwise resumable provider continuity.
- `src/coordinator/services/recovery/service.ts:420` skips `setConversationRef` when `continuity.conversationRef` is an empty string. Simplifying this together with the recovery continuity checks requires deciding whether empty conversation refs are invalid input or intentional "clear/no ref" values.
- `src/coordinator/services/recovery/service.ts:247` maps probe outcomes and mutation state into stale reasons with nested precedence rules. Flattening this needs recovery tests that lock `missing`, `provider_unavailable`, `waiting`, and `pre_checkpoint_*` behavior before changing the control flow.
- `src/providers/codex/request-mapping.ts:120`, `src/providers/codex/request-mapping.ts:131`, `src/providers/claude/exec-provider.ts:185`, `src/providers/claude/exec-provider.ts:189`, and `src/providers/claude/provider-facets.ts:184` use truthiness over continuity string fields. Replacing these with explicit presence checks requires deciding whether empty strings are invalid input or meaningful non-resumable values.
- `src/providers/claude/request-mapping.ts:122`, `src/providers/claude/request-mapping.ts:124`, `src/providers/claude/request-mapping.ts:125`, and `src/providers/claude/request-mapping.ts:142` through `src/providers/claude/request-mapping.ts:153` merge continuity updates with truthy string checks. Flattening those spreads needs the same empty-string decision so persisted broker/session/env fields are not accidentally retained or dropped.
- `src/providers/codex/request-mapping.ts:83` through `src/providers/codex/request-mapping.ts:85` persist continuity updates only when string fields are truthy. Replacing those spreads with explicit `undefined` checks requires deciding whether empty `cwd`, `threadId`, or `turnId` should clear persisted continuity or be rejected upstream.
- `src/providers/bootstrap.ts:198` and `src/providers/bootstrap.ts:216` skip artifact lookup on falsy recovered `conversationRef`/`threadId`. Aligning this with boundary normalization requires deciding whether fallback continuity refs are parsed as non-empty before recovery or rejected as malformed recovery metadata.
- `src/providers/claude/session-kernel.ts:193` through `src/providers/claude/session-kernel.ts:197`, `src/providers/claude/session-kernel.ts:207`, and `src/providers/claude/session-kernel.ts:268` use truthiness for broker/session/turn continuity fields. Migrating them to canonical non-empty refs should happen together with the Claude continuity parser so empty app-server fields cannot silently disappear.
- `src/providers/codex/thread-provider.ts:22`, `src/providers/codex/thread-kernel.ts:177`, `src/providers/codex/thread-kernel.ts:213` through `src/providers/codex/thread-kernel.ts:228`, and the later `threadId`/`turnId` guards in `src/providers/codex/thread-kernel.ts` use truthiness for thread continuity. Replacing these with explicit canonical refs needs a Codex continuity migration that defines how empty streamed IDs are handled.
- `src/coordinator/services/execution-policies.ts:130` suppresses the preserved conversation-ref report with a truthy check. Under the new boundary-normalization rule this should become an explicit canonical `ConversationRef`, but only after empty refs are rejected or normalized at the session/provider boundary.
- `src/coordinator/services/job-launch.ts:212`, `src/coordinator/services/job-launch.ts:324`, and `src/coordinator/services/job-launch.ts:375` rely on truthiness for `sessionId`/`activeJobId`. Tightening these checks requires deciding whether `SessionEntry.activeJobId` and incoming launch `sessionId` are guaranteed non-empty by schema or need a separate normalization pass.
- `src/jobs/store.ts:436` through `src/jobs/store.ts:439` and `src/jobs/shell/launch.ts:164` through `src/jobs/shell/launch.ts:208` build Journal refs with mixed truthiness and explicit null checks. Simplifying them needs a canonical refs builder after job/session/workflow IDs are all normalized as non-empty values.
- `src/sessions/shell.ts:69` preserves an explicit `activeJobId: undefined` property in `normalizeEntry`. Removing that clone/property normalization needs a decision on whether callers rely on own-property shape or only on serialized/session semantics.
- `src/sessions/shell.ts:438`, `src/sessions/shell.ts:448`, `src/sessions/shell.ts:533`, and `src/sessions/shell.ts:577` use `conversationRef: undefined` / `activeJobId: undefined` to clear state after spreading an existing entry. Replacing this with semantic state shape requires updating tests and callers that currently observe the own-property `undefined` shape.
- `src/sessions/shell.ts:370` checkpoints provider continuity only when `update.conversationRef` is truthy. Replacing this with an explicit `undefined` check requires deciding whether an empty conversation ref is invalid input or should mark the session ready.
- `src/discuss/reducer.ts:270` and `src/discuss/state-machine.ts:487` duplicate end-reason fallback precedence. Consolidating the rule requires deciding the canonical owner for forced-end reason resolution between the reducer and state-machine path.

## Optimization Candidates Requiring Logic Decisions

- `src/kb/search/graph-retrieval.ts` rebuilds and JSON-stringifies stable entity graphs during freshness checks. Caching a graph fingerprint or normalized graph needs an invalidation contract tied to index/entity-graph writes so stale structural retrieval cannot be served.
- `src/kb/search/role-registry.ts` freezes a fresh role list on every `list()` call. Caching that view would reduce allocations, but it needs a clear invalidation rule for register/unregister/dispose ordering.
- `src/kb/curate/classification/prompt.ts` still rebuilds entity support and prompt vocabulary while probing `takeClassificationBatchWithIndex`. Incremental vocabulary selection would be faster, but it needs tests that lock batch composition and vocabulary relevance ordering.
- `src/engines/needle/backend.ts` stages all chunks, embedding texts, vectors, and upserts for a snapshot in memory. Streaming or chunked embedding would change batching, partial-failure, and provider call semantics, so it needs an explicit indexing policy.
- `src/kb/corpus/manifest-authority.ts`, `src/kb/corpus/inbound-sync.ts`, and `src/kb/corpus/rescan/drift.ts` can repeat full manifest/hash scans around external mutations. Avoiding those passes needs a freshness model that defines when cached surface hashes are authoritative.
- `src/sessions/lifecycle-reactor.ts` does per-session retention/release lookups while draining lifecycle work. Batching those reads would be faster under many sessions, but it needs ordering guarantees for release events and retention attempts.
- `src/kb/curate/community/detection.ts` performs resolution sweeps and repeated community sorting. Reducing the sweep or caching intermediate graph partitions needs quality thresholds so community topology does not regress silently.
- `src/kb/search/responses.ts` rebuilds community member sets for contextual search responses. Persisting or caching those sets needs invalidation on community document and entity graph updates.
- `src/discuss/persona/dpp.ts` uses dense matrix construction for DPP selection. Replacing it with a sparse or library-backed solver needs numerical tolerances and deterministic tie-breaking rules.
