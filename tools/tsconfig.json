{
  "extends": "../tsconfig/simulation.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".."
  },
  "include": ["provider-proxy-smoke-release-gate.ts", "simulation/**/*.ts", "testing/**/*.ts", "../src/types/**/*.d.ts"],
  "exclude": ["../node_modules", "../dist", "../bridge", "../build"]
}
