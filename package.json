{
  "name": "coral",
  "version": "0.3.11",
  "description": "Claude Code plugin - structured agents with Codex CLI bridge",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/build-server.mjs",
    "build:server": "node scripts/build-server.mjs",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "esbuild": "^0.27.2",
    "typescript": "^5.7.2",
    "vitest": "^4.0.17"
  }
}
